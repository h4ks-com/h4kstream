{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "h4kstream.fullname" . }}-streaming
  labels:
    {{- include "h4kstream.labels" . | nindent 4 }}
spec:
  headers:
    customResponseHeaders:
      X-Accel-Buffering: "no"
{{- end }}
