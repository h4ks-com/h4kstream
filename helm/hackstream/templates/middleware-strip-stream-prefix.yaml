{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "hackstream.fullname" . }}-strip-stream-prefix
  labels:
    {{- include "hackstream.labels" . | nindent 4 }}
spec:
  stripPrefix:
    prefixes:
      - /stream
    forceSlash: false
{{- end }}
