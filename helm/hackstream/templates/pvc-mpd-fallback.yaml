{{- if and .Values.persistence.mpdFallback.enabled .Values.persistence.mpdFallback.create }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "hackstream.fullname" . }}-mpd-fallback
  labels:
    {{- include "hackstream.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - {{ .Values.persistence.mpdFallback.accessMode }}
  {{- if .Values.persistence.mpdFallback.storageClass }}
  storageClassName: {{ .Values.persistence.mpdFallback.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.mpdFallback.size }}
{{- end }}
