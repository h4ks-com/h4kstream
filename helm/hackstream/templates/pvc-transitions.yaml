{{- if and .Values.persistence.transitions.enabled .Values.persistence.transitions.create }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "hackstream.fullname" . }}-transitions
  labels:
    {{- include "hackstream.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - {{ .Values.persistence.transitions.accessMode }}
  {{- if .Values.persistence.transitions.storageClass }}
  storageClassName: {{ .Values.persistence.transitions.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.transitions.size }}
{{- end }}
