{{- if .Values.ingress.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "hackstream.fullname" . }}-block-internal-api
  labels:
    {{- include "hackstream.labels" . | nindent 4 }}
spec:
  ipAllowList:
    sourceRange:
      {{- range .Values.ingress.internalApiWhitelist }}
      - {{ . }}
      {{- end }}
{{- end }}
