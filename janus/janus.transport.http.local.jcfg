general: {
	json = "indented"
	base_path = "/janus"
	http = true
	port = 8100
	https = false
}

admin: {
	admin_http = true
	admin_port = 8101
	admin_base_path = "/admin"
}

# Bind to all interfaces for both HTTP and admin API
ip = "0.0.0.0"

cors: {
	allow_origin = "*"
}
