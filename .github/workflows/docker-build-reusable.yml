name: Reusable Docker Build and Push

on:
  workflow_call:
    inputs:
      image_name:
        required: true
        type: string
      context:
        required: true
        type: string
      dockerfile:
        required: true
        type: string
      docker_hub_username:
        required: true
        type: string
    secrets:
      DOCKER_HUB_TOKEN:
        required: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
        - arch: amd64
          runner: ubuntu-24.04
        - arch: arm64
          runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4

    - uses: docker/setup-buildx-action@v3

    - uses: docker/login-action@v3
      with:
        username: ${{ inputs.docker_hub_username }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}

    - id: meta
      run: echo "short_sha=$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"

    - uses: docker/build-push-action@v6
      with:
        context: ${{ inputs.context }}
        file: ${{ inputs.dockerfile }}
        platforms: linux/${{ matrix.arch }}
        push: true
        provenance: false
        tags: ${{ inputs.docker_hub_username }}/${{ inputs.image_name }}:sha-${{ steps.meta.outputs.short_sha }}-${{ 
          matrix.arch }}
        cache-from: type=registry,ref=${{ inputs.docker_hub_username }}/${{ inputs.image_name }}:cache-${{ matrix.arch 
          }}
        cache-to: type=registry,ref=${{ inputs.docker_hub_username }}/${{ inputs.image_name }}:cache-${{ matrix.arch 
          }},mode=max,image-manifest=true,oci-mediatypes=true

  manifest:
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v4

    - uses: docker/login-action@v3
      with:
        username: ${{ inputs.docker_hub_username }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}

    - id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ inputs.docker_hub_username }}/${{ inputs.image_name }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=sha,prefix=sha-,format=short
          type=raw,value=latest,enable={{is_default_branch}}

    - id: short
      run: echo "sha=$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"

    - name: Assemble multi-arch manifest
      shell: bash
      env:
        IMAGE: ${{ inputs.docker_hub_username }}/${{ inputs.image_name }}
        SHORT_SHA: ${{ steps.short.outputs.sha }}
        TAGS: ${{ steps.meta.outputs.tags }}
      run: |
        src_amd64="${IMAGE}:sha-${SHORT_SHA}-amd64"
        src_arm64="${IMAGE}:sha-${SHORT_SHA}-arm64"
        tag_args=()
        while IFS= read -r t; do
          [ -n "$t" ] && tag_args+=( --tag "$t" )
        done <<< "$TAGS"
        docker buildx imagetools create "${tag_args[@]}" "$src_amd64" "$src_arm64"
        docker buildx imagetools inspect "${IMAGE}:sha-${SHORT_SHA}"
